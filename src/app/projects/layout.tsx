import React from 'react';

export const metadata = { title: 'Projects - Coconut AI' };

export default function ProjectsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
