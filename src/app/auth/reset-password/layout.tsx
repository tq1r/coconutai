import React from 'react';

export const metadata = { title: 'Reset Password - Coconut AI' };

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
