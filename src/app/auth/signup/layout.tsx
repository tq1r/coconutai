import React from 'react';

export const metadata = { title: 'Sign Up - Coconut AI' };

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
